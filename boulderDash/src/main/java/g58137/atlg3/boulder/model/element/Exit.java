package g58137.atlg3.boulder.model.element;

import g58137.atlg3.boulder.model.Position;

/**
 * Represents an Exit.
 * @author Florian Essomba
 */
public class Exit extends Tile {

    /**
     * Constructs a new Exit which initializes the position attribute.
     * @param position the position of the Exit.
     */
    public Exit(Position position){
        super(position);
    }
}
